services:
  ollama:
    image: ollama/ollama
    volumes:
      - ollama_data:/root/.ollama  # persists downloaded models

  docchat:
    build: .
    ports:
      - "7860:7860"
    depends_on:
      - ollama

volumes:
  ollama_data: